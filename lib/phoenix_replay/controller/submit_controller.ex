defmodule PhoenixReplay.SubmitController do
  @moduledoc false
  # Implemented in Phase 2. Finalizes a session into a Feedback record
  # via the configured storage adapter and the host's metadata callback.
end
