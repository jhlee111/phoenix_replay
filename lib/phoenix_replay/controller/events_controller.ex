defmodule PhoenixReplay.EventsController do
  @moduledoc false
  # Implemented in Phase 2. Appends rrweb event batches via the configured
  # storage adapter. Session token + rate limits + body-size cap enforced
  # here.
end
